# T6 验收决策树（Master 判断用）
## T6-A 结果分支
- A1 通道连上+持有≥5min(DISPLAY_INIT发出,收到ACK/PONG并回,uptime达标)：
  → 新证据成立。下一步：派"受控保电验证"——持有SPICE通道跑40min，看是否过40门槛(非120)。
- A2 握手失败(auth code!=0 / REDQ失败 / sub-channel失败)：
  → 定位失败点；若可修(凭据/host/帧序)则修后重试smoke；若不可修则记录根因。
- A3 通道连上但秒断(无ACK/PONG或立即RST)：
  → 协议帧/序列号/心跳间隔问题；评估可修性。
- A4 检测到官方客户端已登录→中止：
  → 需协调独占窗口(无官方客户端时)再测。
## T6-B 结果分支
- B1 原生binary可headless持有会话且不冲突 → 作为A路失败的备选实现路径。
- B2 不可行 → 确认Python raw SPICE为唯一路径。
## 验收红线
- 凭据只引用不写明文；smoke≤10min；禁无新证据跑120min。
- 产物敏感扫描=0；数值进详情核实。
